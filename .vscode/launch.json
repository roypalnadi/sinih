{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run Next.js",
            "type": "node",
            "request": "launch",
            "program": "${workspaceFolder}/node_modules/next/dist/bin/next",
            "args": ["dev"],
            "console": "integratedTerminal",
            "cwd": "${workspaceFolder}"
        }
    ]
}